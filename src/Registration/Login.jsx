import { Helmet } from "react-helmet";

const Login = () => {
    return (
        <div>
    <Helmet>
        <title>Camp Haat - Login</title>
    </Helmet> 
        </div>
    );
};

export default Login;